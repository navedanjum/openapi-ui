{
  "name": "openapi-ui",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "serve": "serve",
    "start": "npm run prepare && node ./dist/index.js && npm run serve",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^14.18.32",
    "gts": "^3.1.1",
    "serve": "^14.0.1",
    "typescript": "~4.7.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">12.0"
  }
}
